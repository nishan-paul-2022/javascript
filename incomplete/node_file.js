/*
fs.existsSync(dir)
fs.mkdirSync(directory);
fs.readFile(fileNameWithdirectory, encoding, errorFirstCallback)
fs.rename(oldPath, newPath, errorFirstCallback)
fs.rename(fileNameWithdirectoryOld, fileNameWithdirectoryNew, errorFirstCallback)
*/

/* 
FILE READ AND WRITE :

    # monolithic approach
        - synchronous
        - asynchronous
            : callback-based  
            : promise-based 

    # modular approach 
*/
